// Providers Component - Template
// Add context providers here

export default function Providers({ children }: { children: React.ReactNode }) {
  return (
    <>
      {/* TODO: Add providers like Redux, Theme, etc. */}
      {children}
    </>
  );
}
